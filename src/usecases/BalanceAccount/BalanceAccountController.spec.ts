import app from '../../shared/infra/http/server';
import request from 'supertest';

describe('Integration Test', () => {
  beforeEach(() => {
    jest.useFakeTimers();
  });

  //it('Balance account', async () => {});
});
